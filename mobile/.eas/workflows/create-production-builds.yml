name: Create production builds

on:
  workflow_dispatch: {}

jobs:
  android-production:
    name: Android production build
    type: build
    params:
      platform: android
      profile: production
